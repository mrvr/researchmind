"""connectors — pluggable search backends for the Similarity Finder.

Each connector implements SearchConnector.search() and returns a list of
CandidatePaper. See documents/similarity-finder-architecture.md.
"""
