certifi==2026.2.25
charset-normalizer==3.4.4
idna==3.11
packaging==26.0
requests==2.32.5
setuptools==82.0.0
urllib3==2.6.3
wheel==0.46.3
fastapi
numpy

# ── Web server ──────────────────────────────────────────────────────────────
uvicorn[standard]
python-multipart      # required by FastAPI File()/Form() upload endpoints

# ── Document / text extraction (processors/) ───────────────────────────────
PyMuPDF               # fitz — PDF text extraction
python-docx            # DOCX
python-pptx            # PPTX
odfpy                  # ODT / ODP
chardet                # encoding detection for text files
striprtf               # RTF stripping

# ── Audio / video transcription (processors/) ──────────────────────────────
faster-whisper

# ── Spreadsheet analysis (processors/spreadsheet_processor.py) ─────────────
pandas

# ── Vector store + embeddings (core/vector_store.py, core/similarity_engine.py) ─
chromadb
sentence-transformers
